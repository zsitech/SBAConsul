spring:
  application:
    name: SbaConsul                 #对应文件夹的名称，即配置文件存放在此文件夹里面
  profiles: 
    active: default                 #对应配置文件
  cloud:
    config:
      uri: http://localhost:10000   #提供配置服务的服务器地址和端口
      


  


    
   
     
     